# NCL Frontend Environment Configuration
# Copy this file to .env.local and fill in your values
#
# NOTE: These are PUBLIC variables bundled into browser code.
# Never put secrets here - use the backend .env for sensitive data.

# =============================================================================
# Supabase Configuration (Required)
# =============================================================================
# Get these from: https://supabase.com/dashboard → Project Settings → API
#
# VITE_SUPABASE_URL: Your Supabase project URL
#   Found at: Settings → API → Project URL
#   Example: https://abcdefghijkl.supabase.co
#
VITE_SUPABASE_URL=https://your-project.supabase.co

# VITE_SUPABASE_ANON_KEY: Public anonymous key for client-side auth
#   Found at: Settings → API → Project API keys → anon public
#   ⚠️ Use the "anon" key here, NOT the "service_role" key!
#   The anon key is safe for browsers - access is controlled by Row Level Security (RLS)
#   Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#
VITE_SUPABASE_ANON_KEY=your-anon-key

# =============================================================================
# API Configuration (Optional)
# =============================================================================
# VITE_API_URL: Backend API server URL
#   Used for: Chat, search, and document processing requests
#   Default: Uses Vite proxy in development (recommended)
#   Set this if: Running backend on a different host/port, or in production
#
VITE_API_URL=http://localhost:8000
