{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: ncl.api.main",
            "type": "debugpy",
            "request": "launch",
            "module": "ncl.api.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}
